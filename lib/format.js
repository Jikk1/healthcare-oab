/* ============================================================
   HealthCareOAB+ — Форматирование и безопасность вывода (без DOM)
   ============================================================
   escapeHtml — единственный барьер против stored-XSS при вставке
   пользовательских/PHI-строк (имена пациентов, MRN, рекомендации, поля
   аудита) в innerHTML. Покрыт unit-тестами.
   ============================================================ */

/**
 * Экранирует строку для безопасной вставки в HTML (включая значения атрибутов:
 * экранируются и кавычки). null/undefined → ''.
 */
export function escapeHtml(value) {
  if (value === null || value === undefined) return '';
  return String(value)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;');
}
