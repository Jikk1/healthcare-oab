{
  "name": "healthcare-oab-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HealthCareOAB+ — фронтенд (лендинг + дашборд врача + ввод анализов), сборка на Vite",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5173",
    "start": "vite",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "chart.js": "^4.4.3",
    "lenis": "^1.1.14"
  },
  "devDependencies": {
    "jsdom": "^24.1.0",
    "vite": "^5.4.10",
    "vitest": "^1.6.0"
  }
}
